FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npx prisma generate --config backend/src/db/prisma/prisma.config.js --schema backend/src/db/prisma/schema.prisma

ENV NODE_ENV=production
ENV PORT=4001

EXPOSE 4001

CMD ["node", "backend/src/index.js"]
